describe('Trips Redux', () => {
	test('Add a trip', () => {
		/* 
		For reference: frontend-bootcamp-2024-09/react-demos/todos/custom-redux/sorting-slice.text.ts
		Create an initial state with no trips
		Create a test trip to add
		Use the action creator addTrip from trips-slice.ts to create an action around the new trip
		Call the reducer with the initial state an the action
		Capture the results
		Write expectations to make sure that the trip was created successfully
		Run tests with "npm test" at the command line/terminal window
		*/
	});
	test('Edit a trip: add a park'); // TODO: write a test
});
